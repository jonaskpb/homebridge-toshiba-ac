/** Platform name used in config.json ("platform" field) and config.schema.json. */
export const PLATFORM_NAME = 'ToshibaAC';

/** Plugin name — must match the npm package name. */
export const PLUGIN_NAME = 'homebridge-toshiba-ac';
